fragment UserBase on User {
	id
	name
	slug
	avatar
	uid
}


query GetMe {
	me {
		...UserBase
	}
}


mutation CreateUser($input: CreateUserInput!) {
  createUser(input: $input) {
	id
	name
	slug
	uid
	avatar
  }
}
